{
  "name": "stratwatch-runner",
  "version": "1.0.0",
  "description": "STRATWATCH intelligence runner — fetches data, writes static JSON",
  "private": true,
  "scripts": {
    "run": "node runner/fetch.js",
    "install-cron": "node runner/install-cron.js",
    "remove-cron": "node runner/remove-cron.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0"
  }
}
